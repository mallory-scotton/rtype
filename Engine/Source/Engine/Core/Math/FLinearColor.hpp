///////////////////////////////////////////////////////////////////////////////
// Header guard
///////////////////////////////////////////////////////////////////////////////
#pragma once

///////////////////////////////////////////////////////////////////////////////
// Dependencies
///////////////////////////////////////////////////////////////////////////////
#include <Engine/Config.hpp>

///////////////////////////////////////////////////////////////////////////////
// Namespace tkd
///////////////////////////////////////////////////////////////////////////////
namespace tkd
{

///////////////////////////////////////////////////////////////////////////////
// Pre-declaration of FColor
///////////////////////////////////////////////////////////////////////////////
class FColor;

///////////////////////////////////////////////////////////////////////////////
/// \brief Class representing a color with linear components
///
///////////////////////////////////////////////////////////////////////////////
class ENGINE_API FLinearColor
{
public:
    ///////////////////////////////////////////////////////////////////////////
    // Class Member
    ///////////////////////////////////////////////////////////////////////////
    union
    {
#ifdef __GNUC__
    #pragma GCC diagnostic push
    #pragma GCC diagnostic ignored "-Wpedantic"
#elif defined(_MSC_VER)
    #pragma warning(push)
    #pragma warning(disable : 4201)
#endif
        struct
        {
            UInt8 r;   //<! Red component
            UInt8 g;   //<! Green component
            UInt8 b;   //<! Blue component
            UInt8 a;   //<! Alpha component
        };
#ifdef __GNUC__
    #pragma GCC diagnostic pop
#elif defined(_MSC_VER)
    #pragma warning(pop)
#endif

        UInt32 color;   //<! Combined color value
    };

public:
    ///////////////////////////////////////////////////////////////////////////
    // Static Class Member
    ///////////////////////////////////////////////////////////////////////////
    static const FLinearColor White;         //<! White color
    static const FLinearColor Black;         //<! Black color
    static const FLinearColor Red;           //<! Red color
    static const FLinearColor Green;         //<! Green color
    static const FLinearColor Blue;          //<! Blue color
    static const FLinearColor Yellow;        //<! Yellow color
    static const FLinearColor Cyan;          //<! Cyan color
    static const FLinearColor Magenta;       //<! Magenta color
    static const FLinearColor Transparent;   //<! Transparent color

public:
    ///////////////////////////////////////////////////////////////////////////
    /// \brief Default constructor, initializes color to black with full
    /// opacity
    ///
    ///////////////////////////////////////////////////////////////////////////
    FLinearColor(void);

    ///////////////////////////////////////////////////////////////////////////
    /// \brief Parameterized constructor to initialize color components
    ///
    /// \param r Red component
    /// \param g Green component
    /// \param b Blue component
    /// \param a Alpha component
    ///
    ///////////////////////////////////////////////////////////////////////////
    FLinearColor(UInt8 r, UInt8 g, UInt8 b, UInt8 a = 255);

    ///////////////////////////////////////////////////////////////////////////
    /// \brief Parameterized constructor to initialize color from a single
    /// UInt32
    ///
    /// \param color Color represented as a single UInt32 value
    ///
    ///////////////////////////////////////////////////////////////////////////
    FLinearColor(UInt32 color);

    //////////////////////////////////////////////////////////////////////////////
    /// \brief Copy constructor to initialize color from an FColor instance
    ///
    /// \param other The FColor instance to copy from
    ///
    /////////////////////////////////////////////////////////////////////////////////
    FLinearColor(const FColor& other);

public:
    ///////////////////////////////////////////////////////////////////////////
    /// \brief Conversion operator to convert FLinearColor to UInt32
    ///
    /// \return Color represented as a single UInt32 value
    ///
    ////////////////////////////////////////////////////////////////////////////
    operator UInt32(void) const;

    ///////////////////////////////////////////////////////////////////////////
    /// \brief Conversion operator to convert FLinearColor to FColor
    ///
    /// \return Converted FColor instance
    ///
    ///////////////////////////////////////////////////////////////////////////
    operator FColor(void) const;

public:
    ///////////////////////////////////////////////////////////////////////////
    /// \brief Converts this linear color to a standard FColor
    ///
    /// \return Converted FColor instance
    ///
    ///////////////////////////////////////////////////////////////////////////
    FColor ToColor(void) const;

public:
    ///////////////////////////////////////////////////////////////////////////
    /// \brief Creates an FLinearColor from a standard FColor
    ///
    /// \param color The FColor to convert
    ///
    /// \return Converted FLinearColor instance
    ///
    ///////////////////////////////////////////////////////////////////////////
    static FLinearColor FromColor(const FColor& color);
};

///////////////////////////////////////////////////////////////////////////////
/// \brief Equality operator for FLinearColor and FLinearColor
///
/// \param lhs The FLinearColor to compare
/// \param rhs The FLinearColor to compare
///
/// \return True if the colors are equal, false otherwise
///
///////////////////////////////////////////////////////////////////////////////
bool operator==(const FLinearColor& lhs, const FLinearColor& rhs);

///////////////////////////////////////////////////////////////////////////////
/// \brief Inequality operator for FLinearColor and FLinearColor
///
/// \param lhs The FLinearColor to compare
/// \param rhs The FLinearColor to compare
///
/// \return True if the colors are not equal, false otherwise
///
///////////////////////////////////////////////////////////////////////////////
bool operator!=(const FLinearColor& lhs, const FLinearColor& rhs);

///////////////////////////////////////////////////////////////////////////////
/// \brief Equality operator for FLinearColor and FColor
///
/// \param lhs The FLinearColor to compare
/// \param rhs The FColor to compare
///
/// \return True if the colors are equal, false otherwise
///
///////////////////////////////////////////////////////////////////////////////
bool operator==(const FLinearColor& lhs, const FColor& rhs);

///////////////////////////////////////////////////////////////////////////////
/// \brief Inequality operator for FLinearColor and FColor
///
/// \param lhs The FLinearColor to compare
/// \param rhs The FColor to compare
///
/// \return True if the colors are not equal, false otherwise
///
///////////////////////////////////////////////////////////////////////////////
bool operator!=(const FLinearColor& lhs, const FColor& rhs);

}   // namespace tkd

///////////////////////////////////////////////////////////////////////////////
// Header guard
///////////////////////////////////////////////////////////////////////////////
#pragma once

///////////////////////////////////////////////////////////////////////////////
// Dependencies
///////////////////////////////////////////////////////////////////////////////
#include <Engine/Assets/UResourceHandle.hpp>
#include <Engine/Config.hpp>
#include <Engine/Core.hpp>
#include <Engine/Renderer/Interfaces/ITexture.hpp>

///////////////////////////////////////////////////////////////////////////////
// Namespace tkd
///////////////////////////////////////////////////////////////////////////////
namespace tkd
{

///////////////////////////////////////////////////////////////////////////////
// Prevent macro conflicts
///////////////////////////////////////////////////////////////////////////////
#ifdef GetCurrentTime
    #undef GetCurrentTime
#endif

///////////////////////////////////////////////////////////////////////////////
/// \brief FlipBook class
///
///////////////////////////////////////////////////////////////////////////////
class UFlipBook : public UObject
{
public:
    ///////////////////////////////////////////////////////////////////////////
    /// \brief Play mode enumeration
    ///
    ///////////////////////////////////////////////////////////////////////////
    enum class EPlayMode
    {
        Once,      //<! Play the flipbook once
        Loop,      //<! Loop the flipbook indefinitely
        PingPong   //<! Play the flipbook forward and backward
    };

    ///////////////////////////////////////////////////////////////////////////
    /// \brief
    ///
    ///////////////////////////////////////////////////////////////////////////
    enum class ESourceMode
    {
        FromFiles,   //<! Load frames from individual files
        FromSprite   //<! Load frames from a sprite sheet
    };

private:
    ///////////////////////////////////////////////////////////////////////////
    // Class Member
    ///////////////////////////////////////////////////////////////////////////
    ESourceMode m_sourceMode;   //<! Source mode for loading frames
    EPlayMode m_playMode;       //<! Play mode of the flipbook
    std::vector<FilePath>
        m_framePaths;           //<! Paths to the frames of the flipbook
    std::vector<FRectangle>
        m_frameRects;           //<! Rectangles for sprite sheet frames
    Float32 m_duration;         //<! Duration of the flipbook animation
    std::vector<FTextureHandle> m_frames;   //<! Loaded frames as textures
    FilePath m_spriteSheetPath;             //<! Path of the current frame
    FTextureHandle m_spriteSheet;           //<! Sprite sheet texture
    Float32 m_currentTime;                  //<! Current time in the animation
    Bool m_isLoaded;                        //<! Whether the frames are loaded
    SizeT m_currentFrameIndex;              //<! Current frame index
    bool m_hardDuration;                    //<! Whether duration is fixed
    Bool m_isPlaying;          //<! Whether the animation is playing
    Bool m_isReversed;         //<! For PingPong mode direction
    Float32 m_playbackSpeed;   //<! Speed multiplier for playback
    Bool m_hasFinished;        //<! Whether Once mode has finished

public:
    ///////////////////////////////////////////////////////////////////////////
    /// \brief Constructor
    ///
    /// \param name Name of the FlipBook
    /// \param sourceMode Source mode for loading frames
    /// \param playMode Play mode of the flipbook
    ///
    ///////////////////////////////////////////////////////////////////////////
    UFlipBook(
        const FString& name = "UFlipBook",
        ESourceMode sourceMode = ESourceMode::FromFiles,
        EPlayMode playMode = EPlayMode::Loop
    );

    ///////////////////////////////////////////////////////////////////////////
    /// \brief Destructor
    ///
    ///////////////////////////////////////////////////////////////////////////
    virtual ~UFlipBook() = default;

public:
    ///////////////////////////////////////////////////////////////////////////
    /// \brief Get the source mode for loading frames
    ///
    /// \return The current source mode
    ///
    ///////////////////////////////////////////////////////////////////////////
    ESourceMode GetSourceMode(void) const;

    ///////////////////////////////////////////////////////////////////////////
    /// \brief Set the source mode for loading frames
    ///
    /// \param mode The source mode to set
    /// \param resetFrames Whether to clear existing frames when changing mode
    ///
    ///////////////////////////////////////////////////////////////////////////
    void SetSourceMode(ESourceMode mode, bool resetFrames = true);

    ///////////////////////////////////////////////////////////////////////////
    /// \brief Get the play mode of the flipbook
    ///
    /// \return The current play mode
    ///
    ///////////////////////////////////////////////////////////////////////////
    EPlayMode GetPlayMode(void) const;

    ///////////////////////////////////////////////////////////////////////////
    /// \brief Set the play mode of the flipbook
    ///
    /// \param mode The play mode to set
    /// \param resetTime Whether to reset the current time to zero
    ///
    ///////////////////////////////////////////////////////////////////////////
    void SetPlayMode(EPlayMode mode, bool resetTime = false);

    ///////////////////////////////////////////////////////////////////////////
    /// \brief Set the frame paths for the flipbook
    ///
    /// \param paths Vector of file paths to set
    ///
    ///////////////////////////////////////////////////////////////////////////
    void SetFramePaths(const std::vector<FilePath>& paths);

    ///////////////////////////////////////////////////////////////////////////
    /// \brief Set the frame rects for the flipbook
    ///
    /// \param rects Vector of texture rects to set
    ///
    ///////////////////////////////////////////////////////////////////////////
    void SetFrameRects(const std::vector<FRectangle>& rects);

    ///////////////////////////////////////////////////////////////////////////
    /// \brief Add a frame to the flipbook
    ///
    /// \param framePath Path to the frame image
    ///
    ///////////////////////////////////////////////////////////////////////////
    void AddFrame(const FilePath& framePath);

    ///////////////////////////////////////////////////////////////////////////
    /// \brief Add a frame to the flipbook
    ///
    /// \param frameRect rectangle of the frame image
    ///
    ///////////////////////////////////////////////////////////////////////////
    void AddFrame(const FRectangle& frameRect);

    ///////////////////////////////////////////////////////////////////////////
    /// \brief Set the duration of the flipbook animation
    ///
    /// \param duration Duration in seconds
    ///
    ///////////////////////////////////////////////////////////////////////////
    void SetDuration(Float32 duration);

    ///////////////////////////////////////////////////////////////////////////
    /// \brief Get the total number of frames in the flipbook
    ///
    /// \return Number of frames
    ///
    ///////////////////////////////////////////////////////////////////////////
    SizeT GetFrameCount(void) const;

    ///////////////////////////////////////////////////////////////////////////
    /// \brief Get the duration of the flipbook animation
    ///
    /// \return Duration in seconds
    ///
    ///////////////////////////////////////////////////////////////////////////
    Float32 GetDuration(void) const;

    ///////////////////////////////////////////////////////////////////////////
    /// \brief Get the paths to the frames of the flipbook
    ///
    /// \return Vector of file paths to the frames
    ///
    ///////////////////////////////////////////////////////////////////////////
    const std::vector<FilePath>& GetFramePaths(void) const;

    ///////////////////////////////////////////////////////////////////////////
    /// \brief Get the loaded frames as textures
    ///
    /// \return Vector of pointers to ITexture representing the frames
    ///
    ///////////////////////////////////////////////////////////////////////////
    const std::vector<FTextureHandle>& GetFrames(void) const;

    ///////////////////////////////////////////////////////////////////////////
    /// \brief Get the path of the current frame
    ///
    /// \return Path to the current frame image
    ///
    ///////////////////////////////////////////////////////////////////////////
    FilePath GetCurrentFramePath(void) const;

    ///////////////////////////////////////////////////////////////////////////
    /// \brief Get the path of a frame at a specific index
    ///
    /// \param index Index of the frame
    /// \return Path to the frame image
    ///
    ///////////////////////////////////////////////////////////////////////////
    FilePath GetFramePath(SizeT index) const;

    ///////////////////////////////////////////////////////////////////////////
    /// \brief Get the path of the frame at a specific time
    ///
    /// \param time Time in seconds
    /// \return Path to the frame image at that time
    ///
    ///////////////////////////////////////////////////////////////////////////
    FilePath GetFramePathAt(Float32 time) const;

    ///////////////////////////////////////////////////////////////////////////
    /// \brief Get the current frame texture
    ///
    /// \return Handle to the current frame texture
    ///
    ///////////////////////////////////////////////////////////////////////////
    FTextureHandle GetCurrentFrame(void) const;

    ///////////////////////////////////////////////////////////////////////////
    /// \brief Get a frame texture at a specific index
    ///
    /// \param index Index of the frame
    /// \return Handle to the frame texture
    ///
    ///////////////////////////////////////////////////////////////////////////
    FTextureHandle GetFrame(SizeT index) const;

    ///////////////////////////////////////////////////////////////////////////
    /// \brief Get the frame texture at a specific time
    ///
    /// \param time Time in seconds
    /// \return Handle to the frame texture at that time
    ///
    ///////////////////////////////////////////////////////////////////////////
    FTextureHandle GetFrameAt(Float32 time) const;

    ///////////////////////////////////////////////////////////////////////////
    /// \brief Get all the rects of an animation
    ///
    /// \return The vector of all rectangles
    ///
    ///////////////////////////////////////////////////////////////////////////
    const std::vector<FRectangle>& GetFrameRects(void) const;

    ///////////////////////////////////////////////////////////////////////////
    /// \brief Get the frame rect at a specific index
    ///
    /// \return The frame rect for a specified index
    ///
    ///////////////////////////////////////////////////////////////////////////
    FRectangle GetFrameRect(SizeT index) const;

    ///////////////////////////////////////////////////////////////////////////
    /// \brief Get a frame rect for a specific time
    ///
    /// \return The frame rect at a specific time
    ///
    ///////////////////////////////////////////////////////////////////////////
    FRectangle GetFrameRectAt(Float32 time) const;

    ///////////////////////////////////////////////////////////////////////////
    /// \brief Get the current frame rect
    ///
    /// \return The current frame rect
    ///
    ///////////////////////////////////////////////////////////////////////////
    FRectangle GetCurrentFrameRect(void) const;

    ///////////////////////////////////////////////////////////////////////////
    /// \brief Load all frames from their file paths
    ///
    /// \return True if frames were loaded successfully, false otherwise
    ///
    ///////////////////////////////////////////////////////////////////////////
    Bool LoadFrames(void);

    ///////////////////////////////////////////////////////////////////////////
    /// \brief Unload all frames from memory
    ///
    ///////////////////////////////////////////////////////////////////////////
    void UnloadFrames(void);

    ///////////////////////////////////////////////////////////////////////////
    /// \brief Check if the frames are currently loaded
    ///
    /// \return True if frames are loaded, false otherwise
    ///
    ///////////////////////////////////////////////////////////////////////////
    Bool IsLoaded(void) const;

    ///////////////////////////////////////////////////////////////////////////
    /// \brief Clear all frames and frame paths
    ///
    ///////////////////////////////////////////////////////////////////////////
    void ClearFrames(void);

    ///////////////////////////////////////////////////////////////////////////
    /// \brief Set the current time in the animation
    ///
    /// \param time Time in seconds to set
    ///
    ///////////////////////////////////////////////////////////////////////////
    void SetCurrentTime(Float32 time);

    ///////////////////////////////////////////////////////////////////////////
    /// \brief Get the current time in the animation
    ///
    /// \return Current time in seconds
    ///
    ///////////////////////////////////////////////////////////////////////////
    Float32 GetCurrentTime(void) const;

    ///////////////////////////////////////////////////////////////////////////
    /// \brief Update the flipbook animation
    ///
    /// \param deltaTime Time elapsed since last update in seconds
    ///
    ///////////////////////////////////////////////////////////////////////////
    void Update(Float32 deltaTime);

    ///////////////////////////////////////////////////////////////////////////
    /// \brief Restart the animation from the beginning
    ///
    ///////////////////////////////////////////////////////////////////////////
    void Restart(void);

    ///////////////////////////////////////////////////////////////////////////
    /// \brief Pause the animation
    ///
    ///////////////////////////////////////////////////////////////////////////
    void Pause(void);

    ///////////////////////////////////////////////////////////////////////////
    /// \brief Resume the animation
    ///
    ///////////////////////////////////////////////////////////////////////////
    void Resume(void);

    ///////////////////////////////////////////////////////////////////////////
    /// \brief Check if the animation is currently playing
    ///
    /// \return True if playing, false if paused
    ///
    ///////////////////////////////////////////////////////////////////////////
    Bool IsPlaying(void) const;

    ///////////////////////////////////////////////////////////////////////////
    /// \brief Check if the animation is currently paused
    ///
    /// \return True if paused, false if playing
    ///
    ///////////////////////////////////////////////////////////////////////////
    Bool IsPaused(void) const;

    ///////////////////////////////////////////////////////////////////////////
    /// \brief Set the playback speed multiplier
    ///
    /// \param speed Speed multiplier (1.0 = normal speed, 2.0 = double speed)
    ///
    ///////////////////////////////////////////////////////////////////////////
    void SetPlaybackSpeed(Float32 speed);

    ///////////////////////////////////////////////////////////////////////////
    /// \brief Get the playback speed multiplier
    ///
    /// \return Current playback speed
    ///
    ///////////////////////////////////////////////////////////////////////////
    Float32 GetPlaybackSpeed(void) const;

    ///////////////////////////////////////////////////////////////////////////
    /// \brief Get the current progress of the animation (0.0 to 1.0)
    ///
    /// \return Progress value between 0.0 and 1.0
    ///
    ///////////////////////////////////////////////////////////////////////////
    Float32 GetProgress(void) const;

    ///////////////////////////////////////////////////////////////////////////
    /// \brief Check if the animation has finished (for Once mode)
    ///
    /// \return True if finished (Once mode only), false otherwise
    ///
    ///////////////////////////////////////////////////////////////////////////
    Bool HasFinished(void) const;

    ///////////////////////////////////////////////////////////////////////////
    /// \brief Set the sprite sheet path (for FromSprite mode)
    ///
    /// \param path Path to the sprite sheet texture
    ///
    ///////////////////////////////////////////////////////////////////////////
    void SetSpriteSheetPath(const FilePath& path);

    ///////////////////////////////////////////////////////////////////////////
    /// \brief Get the sprite sheet path
    ///
    /// \return Path to the sprite sheet texture
    ///
    ///////////////////////////////////////////////////////////////////////////
    const FilePath& GetSpriteSheetPath(void) const;

public:
    ///////////////////////////////////////////////////////////////////////////
    // Class Registration
    ///////////////////////////////////////////////////////////////////////////
    DECLARE_CLASS_WITH_SUPER(UFlipBook, UObject)
};

}   // namespace tkd

///////////////////////////////////////////////////////////////////////////////
// Dependencies
///////////////////////////////////////////////////////////////////////////////
#include <Engine/Renderer/Shapes/UCircleShape.hpp>

///////////////////////////////////////////////////////////////////////////////
// Namespace tkd
///////////////////////////////////////////////////////////////////////////////
namespace tkd
{

///////////////////////////////////////////////////////////////////////////////
UCircleShape::UCircleShape(float radius, SizeT pointCount)
    : UShape()
    , m_radius(radius)
    , m_pointCount(pointCount)
{
    UpdateGeometry();
}

///////////////////////////////////////////////////////////////////////////////
void UCircleShape::SetRadius(float radius)
{
    m_radius = radius;
    UpdateGeometry();
}

///////////////////////////////////////////////////////////////////////////////
TKD_NODISCARD float UCircleShape::GetRadius(void) const { return m_radius; }

///////////////////////////////////////////////////////////////////////////////
void UCircleShape::SetPointCount(SizeT count)
{
    m_pointCount = (count < 3) ? 3 : count;
    UpdateGeometry();
}

///////////////////////////////////////////////////////////////////////////////
TKD_NODISCARD SizeT UCircleShape::GetPointCount(void) const
{
    return m_pointCount;
}

///////////////////////////////////////////////////////////////////////////////
TKD_NODISCARD FVector2f UCircleShape::GetPoint(SizeT index) const
{
    const float angle =
        index * 2.f * M_PI / static_cast<float>(m_pointCount) - M_PI / 2.f;
    return FVector2f(m_radius * std::cos(angle), m_radius * std::sin(angle));
}

///////////////////////////////////////////////////////////////////////////////
TKD_NODISCARD FVector2f UCircleShape::GetGeometricCenter(void) const
{
    return FVector2f(m_radius, m_radius);
}

}   // namespace tkd

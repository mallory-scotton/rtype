///////////////////////////////////////////////////////////////////////////////
// Dependencies
///////////////////////////////////////////////////////////////////////////////
#include <Engine/Renderer/Shapes/UConvexShape.hpp>

///////////////////////////////////////////////////////////////////////////////
// Namespace tkd
///////////////////////////////////////////////////////////////////////////////
namespace tkd
{

///////////////////////////////////////////////////////////////////////////////
UConvexShape::UConvexShape(SizeT pointCount)
    : UShape()
    , m_points()
{
    SetPointCount(pointCount);
}

///////////////////////////////////////////////////////////////////////////////
void UConvexShape::SetPointCount(SizeT count)
{
    m_points.resize(count);
    UpdateGeometry();
}

///////////////////////////////////////////////////////////////////////////////
void UConvexShape::SetPoint(SizeT index, const FVector2f& point)
{
    if (index < m_points.size())
    {
        m_points[index] = point;
        UpdateGeometry();
    }
}

///////////////////////////////////////////////////////////////////////////////
TKD_NODISCARD SizeT UConvexShape::GetPointCount(void) const
{
    return m_points.size();
}

///////////////////////////////////////////////////////////////////////////////
TKD_NODISCARD FVector2f UConvexShape::GetPoint(SizeT index) const
{
    if (index < m_points.size()) { return m_points[index]; }
    return FVector2f::Zero;
}

///////////////////////////////////////////////////////////////////////////////
TKD_NODISCARD FVector2f UConvexShape::GetGeometricCenter(void) const
{
    if (m_points.empty()) { return FVector2f::Zero; }

    FVector2f center = FVector2f::Zero;
    for (const auto& point: m_points) { center += point; }

    return center / static_cast<float>(m_points.size());
}

}   // namespace tkd

///////////////////////////////////////////////////////////////////////////////
// Dependencies
///////////////////////////////////////////////////////////////////////////////
#include <Engine/Renderer/Shapes/UPolygonShape.hpp>

///////////////////////////////////////////////////////////////////////////////
// Namespace tkd
///////////////////////////////////////////////////////////////////////////////
namespace tkd
{

///////////////////////////////////////////////////////////////////////////////
UPolygonShape::UPolygonShape(void)
    : UShape()
    , m_radius(50.f)
    , m_sideCount(6)
{
    UpdateGeometry();
}

///////////////////////////////////////////////////////////////////////////////
UPolygonShape::UPolygonShape(float radius, SizeT sideCount)
    : UShape()
    , m_radius(radius)
    , m_sideCount((sideCount < 3) ? 3 : sideCount)
{
    UpdateGeometry();
}

///////////////////////////////////////////////////////////////////////////////
void UPolygonShape::SetRadius(float radius)
{
    m_radius = radius;
    UpdateGeometry();
}

///////////////////////////////////////////////////////////////////////////////
TKD_NODISCARD float UPolygonShape::GetRadius(void) const { return m_radius; }

///////////////////////////////////////////////////////////////////////////////
void UPolygonShape::SetSideCount(SizeT count)
{
    m_sideCount = (count < 3) ? 3 : count;
    UpdateGeometry();
}

///////////////////////////////////////////////////////////////////////////////
TKD_NODISCARD SizeT UPolygonShape::GetSideCount(void) const
{
    return m_sideCount;
}

///////////////////////////////////////////////////////////////////////////////
TKD_NODISCARD SizeT UPolygonShape::GetPointCount(void) const
{
    return m_sideCount;
}

///////////////////////////////////////////////////////////////////////////////
TKD_NODISCARD FVector2f UPolygonShape::GetPoint(SizeT index) const
{
    const float angle =
        index * 2.f * M_PI / static_cast<float>(m_sideCount) - M_PI / 2.f;
    const float x = m_radius + m_radius * std::cos(angle);
    const float y = m_radius + m_radius * std::sin(angle);
    return FVector2f(x, y);
}

///////////////////////////////////////////////////////////////////////////////
TKD_NODISCARD FVector2f UPolygonShape::GetGeometricCenter(void) const
{
    return FVector2f(m_radius, m_radius);
}

}   // namespace tkd

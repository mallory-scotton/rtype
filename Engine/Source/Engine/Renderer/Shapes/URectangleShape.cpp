///////////////////////////////////////////////////////////////////////////////
// Dependencies
///////////////////////////////////////////////////////////////////////////////
#include <Engine/Renderer/Shapes/URectangleShape.hpp>

///////////////////////////////////////////////////////////////////////////////
// Namespace tkd
///////////////////////////////////////////////////////////////////////////////
namespace tkd
{

///////////////////////////////////////////////////////////////////////////////
URectangleShape::URectangleShape(void)
    : UShape()
    , m_size()
{
    SetSize(FVector2f::Zero);
}

///////////////////////////////////////////////////////////////////////////////
URectangleShape::URectangleShape(const FVector2f& size)
    : UShape()
    , m_size()
{
    SetSize(size);
}

///////////////////////////////////////////////////////////////////////////////
void URectangleShape::SetSize(const FVector2f& size)
{
    m_size = size;
    UpdateGeometry();
}

///////////////////////////////////////////////////////////////////////////////
TKD_NODISCARD const FVector2f& URectangleShape::GetSize(void) const
{
    return m_size;
}

///////////////////////////////////////////////////////////////////////////////
TKD_NODISCARD SizeT URectangleShape::GetPointCount(void) const { return 4; }

///////////////////////////////////////////////////////////////////////////////
TKD_NODISCARD FVector2f URectangleShape::GetPoint(SizeT index) const
{
    switch (index)
    {
    case 0 : return FVector2f(0.0f, 0.0f);
    case 1 : return FVector2f(m_size.x, 0.0f);
    case 2 : return FVector2f(m_size.x, m_size.y);
    case 3 : return FVector2f(0.0f, m_size.y);
    default: return FVector2f(0.0f, 0.0f);   // Should never happen
    }
}

///////////////////////////////////////////////////////////////////////////////
TKD_NODISCARD FVector2f URectangleShape::GetGeometricCenter(void) const
{
    return m_size * 0.5f;
}

}   // namespace tkd

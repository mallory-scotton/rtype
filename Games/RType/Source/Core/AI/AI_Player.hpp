///////////////////////////////////////////////////////////////////////////////
// Header guard
///////////////////////////////////////////////////////////////////////////////
#pragma once

///////////////////////////////////////////////////////////////////////////////
// Dependencies
///////////////////////////////////////////////////////////////////////////////
#include <Core/Player/BP_Player.hpp>
#include <Engine/Core/Math.hpp>

///////////////////////////////////////////////////////////////////////////////
// Namespace tkd
///////////////////////////////////////////////////////////////////////////////
namespace tkd
{

///////////////////////////////////////////////////////////////////////////////
/// \brief Player class, derived from BP_Player
///
///////////////////////////////////////////////////////////////////////////////
class AI_Player : public BP_Player
{
public:
    ///////////////////////////////////////////////////////////////////////////
    // Class members
    ///////////////////////////////////////////////////////////////////////////
    UProperty<Float32> speed;        //<! The movement speed of the player
    UProperty<FVector2f> velocity;   //<! The current velocity of the player
    UProperty<UInt32> playerColor;   //<! The color index of the player

    bool m_isMovementInitialized = false;
    Float32 m_totalTime = 0.0f;
    Float32 m_waveFrequency = 2.0f;     // Vitesse de l'oscillation
    Float32 m_waveAmplitude = 300.0f;   // Hauteur de l'oscillation
    Float32 m_forwardSpeed = -1.0f;     // Vitesse de "descente" (ex: -1 en X)
    Float32 m_startY;                   // Position Y de départ
    UFunction<> ServerFire;             //<! Server RPC to handle firing
    UFunction<FTransform> MulticastFire;   //<! Multicast RPC to handle firing

private:
    ///////////////////////////////////////////////////////////////////////////
    // Class Member
    ///////////////////////////////////////////////////////////////////////////
    FVector2f m_lastVelocity;      //<! Last movement direction
    Float32 m_lastFiredTime;       //<! Time since last fired shot
    FVector3 m_lastPosition;       //<! Last position for velocity calculation

    FB_ShipIdle m_idleAnimation;   //<! Idle flipbook
    FB_ShipMoveUp m_moveUpAnimation;       //<! Move up flipbook
    FB_ShipMoveDown m_moveDownAnimation;   //<! Move down flipbook

public:
    ///////////////////////////////////////////////////////////////////////////
    /// \brief Default constructor
    ///
    /// \param playerColor The color index of the player (default is 0)
    ///
    ///////////////////////////////////////////////////////////////////////////
    AI_Player(UInt32 playerColor = 0);

public:
    ///////////////////////////////////////////////////////////////////////////
    /// \brief Called when the object is first created
    ///
    ///////////////////////////////////////////////////////////////////////////
    virtual void BeginPlay(void) override;

    ///////////////////////////////////////////////////////////////////////////
    /// \brief Ticks the object
    ///
    /// \param deltaTime The time elapsed since the last tick
    ///
    ///////////////////////////////////////////////////////////////////////////
    virtual void Tick(Float32 deltaTime) override;

protected:
    ///////////////////////////////////////////////////////////////////////////
    /// \brief Simulate movement with player-specific logic
    ///
    /// \param inputVector The input vector for this move
    /// \param deltaTime The time elapsed for this move
    /// \param startTransform The starting transform
    ///
    /// \return The new transform after applying movement
    ///
    ///////////////////////////////////////////////////////////////////////////
    virtual FTransform SimulateMovement(
        const FVector3& inputVector,
        Float32 deltaTime,
        const FTransform& startTransform
    ) override;

public:
    ///////////////////////////////////////////////////////////////////////////
    // Class metadata
    ///////////////////////////////////////////////////////////////////////////
    DECLARE_CLASS_WITH_SUPER(AI_Player, BP_Player)
};

}   // namespace tkd

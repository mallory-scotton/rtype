#pragma once

///////////////////////////////////////////////////////////////////////////////
// Dependencies
///////////////////////////////////////////////////////////////////////////////
#include <Core/Boss/FlipBook/FB_BossIdle.hpp>
#include <Engine.hpp>

///////////////////////////////////////////////////////////////////////////////
// Namespace tkd
///////////////////////////////////////////////////////////////////////////////
namespace tkd
{

///////////////////////////////////////////////////////////////////////////////
/// Simple boss actor: flipbook-driven animation state machine
/// (idle/move/attack/hurt/dead)
///////////////////////////////////////////////////////////////////////////////
class BP_Boss : public AActor
{
public:
    ///////////////////////////////////////////////////////////////////////////
    // Configurable properties
    ///////////////////////////////////////////////////////////////////////////
    UProperty<Float32> attackRange;      // distance to trigger attack
    UProperty<Int32> health;             // boss health
    UProperty<Float32> attackCooldown;   // seconds between attacks

private:
    ///////////////////////////////////////////////////////////////////////////
    // Runtime state
    ///////////////////////////////////////////////////////////////////////////
    Float32 m_attackTimer;
    Bool m_isAttacking;
    Bool m_isDead;

    FB_BossIdle m_idleAnimation;

public:
    BP_Boss(const UUID& uuid = UUID::V4());

    virtual void BeginPlay(void) override;
    virtual void Tick(Float32 deltaTime) override;

    // External API
    void StartAttack(void);
    void TakeDamage(Int32 amount);

private:

public:
    DECLARE_CLASS_WITH_SUPER(BP_Boss, AActor)
};

}   // namespace tkd
